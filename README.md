# book-shop

## Installation
1) Create .env in main directory with:

```dotenv
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DJANGO_SECRET_KEY=
DEBUG=
```

2) 
```bash
docker-compose build
```

3) 
```bash
docker-compose up
```